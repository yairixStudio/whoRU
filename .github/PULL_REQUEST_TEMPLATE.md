## What

One or two sentences. Link the issue if there is one.

## Why

## How I tested it

- [ ] `swift build` and `swift test` pass locally
- [ ] Tested against a real permission dialog (which app / permission?)
- [ ] Added or updated tests for `WhoRUCore` changes

## Checklist

- [ ] Does not click, type or answer dialogs on the user’s behalf
- [ ] Does not send file contents off the machine
- [ ] Does not let the model override the hard score
- [ ] No new third-party dependency (or discussed in an issue first)
- [ ] Commands are invoked with argument arrays, never shell strings
